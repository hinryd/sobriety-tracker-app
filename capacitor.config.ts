import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hinry.sobriety',
  appName: 'sobriety-tracker-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
